# Netlify 배포 설정
# GitHub 저장소를 Netlify에 연결하는 경우, 이 폴더(anatomylab-3d)를
# "Base directory"로 지정하세요. (저장소 루트에 프로젝트가 있다면 base 불필요)

[build]
  command = "npm run build"
  publish = "dist"

# SPA 라우팅: 모든 경로를 index.html 로 (새로고침 404 방지)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
